name: Keep Render Awake

on:
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: |
          curl -sS -o /dev/null -w "%{http_code}\n" https://ev-charging-and-services-booking.onrender.com/
